name: yaml/list-remove-by-value-duplicate
seams: [e2e]
kind: error
format: yaml
error: HEW012
error_seam: apply-ir
error_path: /tags/#hew:sha256=a5bdd2784405a5f75fddf09a07a373f3c8f1a4cdd5f463e28f74a44e1f4215ea
patch_line: 6
message_contains: ["ambiguous-match", "collide"]
ops: [OP-15]
why: |
  A duplicated scalar in a list is drift. A set member is addressed by a hash of
  its value (satisfied-recoil), so two equal members share a digest: hew names the
  COLLISION and refuses rather than picking the first match — the addressing half
  of the loud-staleness discipline. The colliding value is not in the message (the
  digest is one-way), but the `- beta` line it came from is patch line 6.
spec: "§4.2, §4.5c, §11 OP-15"
